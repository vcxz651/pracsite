# 데모 튜토리얼 지원자 칩 레이아웃 메모

2026-03-01 기준 `demo_feature_tutorial` 안의 지원자 칩은 아래 규칙으로 정리했다.

- 텍스트는 한 줄 이름만 표시
- `지원중` 같은 보조 상태 텍스트는 표시하지 않음
- 이름은 칩 중앙 정렬
- 클릭 가능한 경우에도 버튼은 칩 전체 폭을 차지하고 텍스트만 중앙 정렬

이 레이아웃은 데모 튜토리얼에서 먼저 검증하고, 추후 `meeting_detail`의 실제 지원자 카드/칩 UI를 다듬을 때 참고용으로 재사용한다.
